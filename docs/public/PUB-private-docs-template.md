# 本地私有文档模板（不入库）

本文件用于说明私有文档约定；实际私有内容请写在被 `.gitignore` 忽略的文档中。

## 适合放私有文档的内容

- 企业内部业务规则（非公开）
- 生产环境口径、账号与运维路径
- 内部 SOP、组织协作流程、群路由信息
- 任何可能暴露客户/渠道/系统细节的内容

## 管理约定

1. 私有文档路径固定在 `docs/` 下，但文件名在 `.gitignore` 中列出。
2. 新增私有文档时，同步更新 `.gitignore` 与 `docs/README.md`。
3. 若要对外分享，先抽象为无敏感信息的公开版，再新增到公开文档列表。
